- hosts: 127.0.0.1
  connection: local
  become: true
  vars_prompt:
    - name: install_drive
      prompt: "Enter Installation Drive (e.g. sda)"
      private: no
  tasks:
  - name: Install htop
    pacman:
      name: htop
      state: present
  - debug: msg={{ install_drive }}

