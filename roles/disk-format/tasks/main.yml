- name: "Install gptfdisk package"
  pacman:
    name: gptfdisk
    state: present

- name: Format first disk with boot and zfs partitions
  include_task:
    file: format_single_disk.yml
    apply:
      vars:
        disk_device: "{{ disk_format_def_device_1 }}"

